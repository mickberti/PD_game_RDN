import { RdnCatalogueContract } from "../catalogue.contract";
import { RDN_RELEASE } from "./rdn-release.config";

export const RDN_CATALOGUE_CONTRACT: RdnCatalogueContract = {
  version: "v004",
  levelSchemaVersion: 1,
  generatorVersion: RDN_RELEASE.generatorVersion,
};
