# Dark Classic audio pack

This is the current Classic set, moved from the former `assets/audio/music` and
`assets/audio/sfx` roots. It is selected by default through the `classic` pack id.

Keep its file layout identical to the other packs so cue paths can remain semantic and shared.
